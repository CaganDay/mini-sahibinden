spring.application.name=MiniSahibinden

# ============================================
# RELATIONAL SQL DATABASE CONFIGURATION (MySQL)
# ============================================
# Database Connection - MySQL (Relational SQL Database)
spring.datasource.url=jdbc:mysql://localhost:3306/MiniSahibinden?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Merrow4863
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Settings
# Explicitly set MySQL dialect for SQL generation
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# This updates your DB schema if you change your Java code (Safe for dev)
spring.jpa.hibernate.ddl-auto=create 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true